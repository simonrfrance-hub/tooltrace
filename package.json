{
  "name": "localizit",
  "version": "0.1.0",
  "private": true,
  "description": "Localizit — live asset tracking: webhook ingest + geofence/movement rules engine + live map dashboard",
  "type": "module",
  "scripts": {
    "build": "node scripts/gen-dashboard.js",
    "start": "node local.js",
    "dev": "node --watch local.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
