{
  "name": "tooltrace",
  "version": "0.1.0",
  "private": true,
  "description": "BLE tracker platform prototype: webhook ingest + geofence/movement rules engine + live map dashboard",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "express": "^4.21.2"
  }
}
